# range creates an array  range函数创建了一个数组
# First parameter is the starter  第一个参数是起始数
# Second indicates the number of numbers to create 第二个是需要创建的元素个数 
# range(0, 2) creates [0, 1]  range(0,2) 将创建列表 [0,1]
for index in range(0, 2):
	print(index)
